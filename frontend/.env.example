# Desenvolvimento local (opcional — sem isso usa proxy /api do Vite)
# VITE_API_URL=http://localhost:3001/api

# Produção (Netlify): URL do backend no Render + /api
# VITE_API_URL=https://jogo-soletrando-api.onrender.com/api
